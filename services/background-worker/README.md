# Background Worker

.NET worker scaffold for outbox, projections, MISA retry/reconcile, alerts and async jobs.

Current scope:

- No background job is implemented.
- Future work must consume outbox/integration boundaries rather than call business modules directly.
