# Operational API

ASP.NET Core API scaffold for Ginsengfood Operational V2.

Current scope:

- Only `/health` exists.
- CODE01 routes, DTOs and services are not implemented yet.
- Business modules must follow `docs/software-specs/api/` and `docs/software-specs/modules/`.
