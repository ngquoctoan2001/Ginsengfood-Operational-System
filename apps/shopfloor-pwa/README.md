# Shopfloor PWA

Next.js App Router scaffold for operator workflows.

Current scope:

- No offline command, scan, or workflow feature is implemented.
- Future commands must use idempotency and backend validation.
- Material issue must never rely on UI state as business truth.
