# Public Trace

Next.js App Router scaffold for anonymous public trace.

Current scope:

- No public trace API integration is implemented.
- Future UI must call only `/api/public/trace/{qrCode}`.
- Internal supplier, personnel, costing, QC defect/loss, MISA and private data must never be rendered.
