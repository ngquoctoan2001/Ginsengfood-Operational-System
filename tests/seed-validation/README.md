# Seed Validation Tests

Seed verification and fixture guard tests.

Use this folder for:

- canonical seed idempotency checks;
- G1/SKU/ingredient/master-data acceptance queries;
- test fixture isolation checks;
- validation that dev fixtures are not mixed into production seed chains.
